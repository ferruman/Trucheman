import { readFileSync } from "node:fs";
import { resolve } from "node:path";
export type SecretStore = Readonly<{ translationApiKey?:string; editingApiKey?:string; translationEndpoint?:string; editingEndpoint?:string; translationModel?:string; editingModel?:string }>;
export function loadSecrets(path?:string): SecretStore { let text=""; for(const candidate of path?[path]:[resolve(process.cwd(),".env.local"),resolve(process.cwd(),".env")]){try{text=readFileSync(candidate,"utf8");break;}catch{continue;}} if(!text)return Object.freeze({}); const values:Record<string,string>={}; for (const line of text.split(/\r?\n/)) { const m=line.match(/^\s*([A-Z0-9_]+)\s*=\s*(.*)\s*$/); if(m) values[m[1]]=m[2].replace(/^['"]|['"]$/g,""); } return Object.freeze({translationApiKey:values.BOOK_TRANSLATOR_TRANSLATION_API_KEY||undefined,editingApiKey:values.BOOK_TRANSLATOR_EDITING_API_KEY||undefined,translationEndpoint:values.BOOK_TRANSLATOR_TRANSLATION_ENDPOINT||undefined,editingEndpoint:values.BOOK_TRANSLATOR_EDITING_ENDPOINT||undefined,translationModel:values.BOOK_TRANSLATOR_TRANSLATION_MODEL||undefined,editingModel:values.BOOK_TRANSLATOR_EDITING_MODEL||undefined}); }
