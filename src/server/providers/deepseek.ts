import { buildPromptMessages } from "./prompts.js";
import { ProviderError, type LanguageModelProvider, type ProviderRequest, type ProviderResponse } from "./provider.js";
import { validateProviderResponse } from "./response-validator.js";
export class DeepSeekProvider implements LanguageModelProvider { async complete(request:ProviderRequest,signal?:AbortSignal):Promise<ProviderResponse>{if(!request.profile.apiKey)throw new ProviderError("configuration","Provider credential is not configured");const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),request.profile.timeoutMs??60000);if(signal)signal.addEventListener("abort",()=>controller.abort(),{once:true});try{const res=await fetch(request.profile.endpoint,{method:"POST",headers:{"content-type":"application/json",authorization:`Bearer ${request.profile.apiKey}`},body:JSON.stringify({model:request.profile.model,messages:buildPromptMessages(request),response_format:{type:"json_object"},stream:false}),signal:controller.signal});const requestId=res.headers.get("x-request-id")??undefined;if(!res.ok){if(res.status===401||res.status===403||res.status===400)throw new ProviderError("configuration",`Provider rejected configuration (${res.status})`,res.status);throw new ProviderError("temporary",`Provider temporarily unavailable (${res.status})`,res.status);}let body:any;try{body=await res.json();}catch{throw new ProviderError("invalid_response","Provider returned invalid JSON");}const content=body?.choices?.[0]?.message?.content;if(typeof content!=="string"||!content.trim())throw new ProviderError("invalid_response","Provider returned an empty response");let parsed:any;try{parsed=JSON.parse(content);}catch{throw new ProviderError("invalid_response","Provider returned malformed structured output");}try{return validateProviderResponse({segments:parsed.segments,finishReason:body.choices?.[0]?.finish_reason,requestId,usage:{promptTokens:body.usage?.prompt_tokens,completionTokens:body.usage?.completion_tokens}},request.segments);}catch(e){throw new ProviderError("invalid_response",e instanceof Error?e.message:"Invalid provider response");}}catch(e){if(e instanceof ProviderError)throw e;if((e as Error).name==="AbortError")throw new ProviderError("temporary","Provider request timed out");throw new ProviderError("temporary","Provider request failed");}finally{clearTimeout(timer);}}
}
