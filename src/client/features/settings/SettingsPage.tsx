export function SettingsPage() {
  return (
    <section>
      <h2>Provider settings</h2>
      <p>Credentials are loaded by the server and only credential presence is displayed.</p>
    </section>
  );
}
